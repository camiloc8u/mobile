import 'package:flutter/material.dart';

/// Colores compartidos de la app GymZone.
///
/// Antes estas constantes estaban duplicadas dentro de varias pantallas
/// (HomeScreen, FitMenuScreen, ProfileScreen, etc). Ahora viven en un solo
/// lugar para que todas las pantallas usen exactamente el mismo verde y el
/// mismo fondo de tarjeta.
const Color kGreenColor = Color(0xFF90EE00);
const Color kCardBg = Color(0xFF18181B);
